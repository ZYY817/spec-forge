---
name: requirement-anchor
description: Turn free-form context, meeting notes, screenshots, files, or project details into clear, appropriately scoped, actionable, and testable product requirements. Supports requirement drafting, review, refinement, and implementation planning in the user's language, with strong fact extraction for noisy meeting transcripts.
---

# 需求定锚器 / Requirement Anchor

## 核心目标

把用户的自然输入转化为产品、运营、开发、测试都能理解的需求单。优先服务日常工作写需求；只有用户明确涉及开发或 AI Coding 时，才按需补充技术实现、架构和开发计划。

固定思考原则，不固定文档模板：内容、篇幅和表达形式必须随需求复杂度变化。

## 工作流程

### 0. 判断当前处于哪个阶段

先判断用户是在：

- 提供背景或材料
- 讨论问题和方案
- 确认结论
- 要求生成需求单
- 要求补充实现规划或开始开发

如果用户只是提供材料或表示想讨论，不要主动生成正式需求单。讨论阶段的输出应是当前理解、关键判断和必要问题；只有用户确认要写或已明确要求写时，才进入文档阶段。

### 1. 先理解输入

接受任意形式的输入，包括一句话、背景说明、聊天记录、截图、文件、旧需求和项目代码。优先使用用户已提供的信息，不重复询问已经明确的内容。

先整理出：

- 已知背景和事实
- 用户想解决的问题
- 用户提出的方案或倾向
- 仍可能影响结论的关键信息

如果截图或页面信息改变了对业务的理解，必须以截图和用户补充说明为准，不要只依据一句话下结论。

### 1.5. 会议记录和语音转录的事实提取

当输入来自会议记录、聊天记录或语音转录时，必须先做一次“事实提取”，再开始写需求。不要直接把原始发言改写成需求。

先单独整理会议中已经确认的内容，至少包括：

- 已确认的现状和问题
- 已确认的处理结论
- 已明确的需求范围
- 已明确的负责人、协作方或后续动作

每条内容都要能在原始材料中找到依据。对于“可以、需要、确定、统一、拆成两个”等明确表态，按确认结论处理；对于“可能、估计、我理解、要看看、待确认”等表述，不得直接写成确定需求。

如果会议中出现多个方案，先判断最终是否形成结论：

- 已被参会者明确接受或作为后续动作安排的，才进入当前需求；
- 仍在比较、评估或等待确认的，放入待确认事项；
- 明确属于未来迁移、重构或长期规划的，放入后续方向，不得混入本次交付范围。

### 1.6. 语音识别错误和术语校正

语音转录可能包含同音错词、断句错误、说话人混淆和产品名误识别。处理时不得机械照抄，也不得凭常识擅自替换。

按以下优先级校正术语：

1. 用户明确补充或纠正的名称；
2. 同一段会议中反复出现、且上下文一致的名称；
3. 已知页面、接口、系统或业务对象名称；
4. 无法确认时，保留原词并标记“术语待确认”，不要自行定稿。

对影响需求范围、接口、系统、业务对象或验收结果的术语，必须在生成正式需求前确认含义。若只是明显的错别字且不影响理解，可以在需求中使用校正后的说法，但不要把校正推断描述成会议原话。

### 2. 先讨论，再成文

不要在业务关系尚未理解、方案仍有争议时直接输出正式需求单。先说明当前理解，指出表面方案是否可能没有解决根本问题，并和用户确认方向。

只在缺失信息会明显改变结果时提问。优先一次提出一个最关键的问题；如果可以先形成合理初稿，则先写出已知内容并明确标注假设或待确认事项。

用户确认结论后，按最新结论更新文档。已经确认的内容不得继续写成“待确认”。

### 2.5. 管理不确定性

不要为了让文档完整而猜测业务规则。把内容分为事实、用户已确认结论、合理假设和待确认事项。假设会改变功能、范围或数据结果时，必须先和用户确认；不会改变初稿方向时，可以先继续并明确标注。

如果用户在讨论中推翻前一个方案，以最新结论为准，同时清理文档中与新结论冲突的旧内容，不要把多个互相冲突的方案都当成当前需求。

### 2.6. 需求生成前的结论闸门

在从会议或讨论材料生成需求单前，必须先完成一次核对：

- 需求背景是否来自已确认事实；
- 需求目标是否是明确要解决的问题；
- 每项需求内容是否有原始材料依据；
- 是否把“可能、建议、我理解、后续规划”误写成当前需求；
- 是否把确定事实不必要地弱化为“可能”；
- 是否存在同一术语在不同段落含义不一致的情况。

如果关键事实仍不清楚，先输出当前理解、事实依据和一个关键待确认问题；如果不影响形成初稿，可以继续写，但必须明确标注待确认事项。

### 2.7. 歧义闸门

当“提示、同步、复制、关联、替换、继承、映射”等说法可能同时代表页面行为和数据行为时，先区分两者。如果不同理解会导致不同实现、数据关系或验收结果，不得直接生成正式需求单；先输出当前理解、分歧点和一个关键问题。

### 3. 判断输出深度

根据实际内容选择必要章节，不要求每次完整覆盖所有维度。

- 简单需求：背景、需求详情、关键验收即可。
- 一般需求：增加页面位置、流程、业务规则、异常情况等必要内容。
- 复杂需求：再增加角色、状态流转、数据关系、映射关系或实现说明。

复杂度只决定文档写到什么程度，不决定用户必须使用哪种固定模式。

### 4. 选择合适的表达形式

不要全部使用表格或流程图。根据理解效果选择：

- 普通背景和规则使用简洁文字
- 多个对象或前后关系使用表格
- 多步骤、多角色流程使用流程图
- 状态变化使用状态流转图
- 页面入口和操作路径使用页面路径或步骤说明
- 简单内容直接用几段话说明

可参考 [表达形式与模板](references/format-selection.md)。

处理截图时，分别记录每张截图中直接可见的页面、状态、对象、组件和操作入口，并区分截图事实、用户说明和模型推断。不能仅凭两张截图就默认对象存在关联。页面位置除了页面路径，还要写视觉位置或组件名称，例如“复制确认弹窗说明文字下方”或“商品售卖物品组合区域的物品选择字段”。

### 5. 生成需求单

需求单至少要让读者知道：

- 为什么做
- 要解决什么问题
- 在哪里操作
- 用户怎么操作
- 系统最终应该怎样
- 哪些内容保持现有逻辑
- 如何验收

页面需求要写清入口和位置，例如：`商品中心 → 商品组管理 → 商品列表 → 操作列 → 复制 → 确认弹窗`。不要只写“增加提示”或“修改逻辑”。

涉及复制、关联、替换或数据同步时，必须明确原对象、新对象、关联位置和最终结果。不要只写“增加映射”。

只有在该关系会影响实现或验收时，才进一步说明关系类型、触发时机、处理方向、是否持久化和冲突处理，不要把这些内容强行加入简单需求。

需求单的表达形式可以根据用户用途和需求复杂度自由选择，不规定固定章节、固定顺序或固定模板。无论采用何种形式，都必须让读者区分已确认事实、当前要做的内容、待确认事项和后续规划。

如果材料来自会议，正式需求单前可以先整理会议确认结论作为依据；用户只要求最终需求单时，也可以将这些结论融入背景和需求说明，不必机械增加独立章节。

### 6. 补充边界和验收

只覆盖与当前需求相关的关键情况，至少检查正常路径和明显异常路径。重点关注空数据、多个对象、重复操作、权限、失败、关联错误和数据独立性。

验收标准要描述可观察结果，避免使用“体验更好”“处理更合理”等不可验证表述。可参考 [需求检查清单](references/requirements-review-checklist.md)。

### 7. 交付前自检

输出前做一次轻量检查：文档是否写清问题、位置、核心逻辑、范围、异常和验收；篇幅是否与需求复杂度匹配；是否存在重复、猜测、冲突或无关内容。若缺少关键信息，优先说明缺口，不要用大量泛化内容填充。

默认输出最小必要内容。用户只要求讨论时，输出讨论结论；用户只要求需求单时，省略 AI Coding 规划；用户明确要求实现时，再把技术内容作为独立的实现规划或开发附录放在业务需求之后。

根据用户的用途选择结果形态：快速同步、正式需求单、开发测试版或 AI Coding 实现规划。没有明确要求时，优先提供适合当前输入的最小版本，不主动输出多套重复文档。用户提供图片或附件时，按需在对应章节引用，并说明图片是背景参考、交互说明还是验收依据。

如果用户是在修改已有需求，先识别新增、删除和变更内容，并说明对范围、页面、数据、任务或验收的影响；不要把旧方案和新方案混成当前结论。根据需求判断是否需要优先级、依赖、版本记录或明确不做项。

按需判断非功能需求：涉及权限、敏感数据、性能、稳定性、审计、兼容性、迁移、发布或回滚时才补充，不要对简单需求机械添加。

如果需求已经进入实现或验收阶段，增加一次反向检查：假设开发人员只看这份文档，是否仍可能误解页面位置、对象关系、范围或验收结果；如果已经有实现，则对照需求列出已完成、未完成和偏差。

## AI Coding 扩展

当用户明确要求实现功能、让 AI Coding 开发，或需要从 0 到 1 完成产品时，在业务需求之后按需补充：

- 现有项目和技术栈分析
- 技术选型及理由
- 页面、组件和业务模块拆分
- 数据、接口和状态管理
- 代码质量和复用要求
- 开发顺序与分阶段任务
- 测试、验证和交付标准

不要在普通工作需求中默认堆叠框架、接口和架构。已有项目优先遵循项目现状；信息不足时说明假设，不要擅自确定技术栈。

## 输出原则

- 使用用户指定的语言；如果用户未指定，跟随输入材料的主要语言。表达要直白，让产品、运营、开发和测试都能看懂。
- 精简表达，但保留影响开发和验收的关键逻辑。
- 不为了显得专业而添加无关章节、术语或修饰语。
- 不重复用户已经提供的背景。
- 区分事实、假设、已确认结论和待确认事项。
- 区分当前交付范围与后续优化方向。
- 需求单是业务沟通和后续实现的基线，不是一次性漂亮文案。
- 不展示冗长的内部推理过程，只给用户当前理解、关键判断、结论、问题和结果。
- 基础检查项是质量标准，不是每份文档都必须出现的章节。
- 可以只输出一段结论、一个表格或一个流程，不能为了套结构而扩写。
- 图片、附件、版本记录、优先级和非功能要求都是按需能力，不是固定章节。

## 参考资料

- 需要选择文档结构、表格或图示时，读取 [表达形式与模板](references/format-selection.md)。
- 需要检查需求是否清楚、完整、可开发和可验收时，读取 [需求检查清单](references/requirements-review-checklist.md)。
- 需要衔接 AI Coding 时，读取 [AI Coding 衔接指南](references/ai-coding-bridge.md)。
- 需要回顾设计原则和开源项目精髓时，读取 [设计原则与来源](references/design-principles.md)。
- 需要验证 Skill 行为时，参考 [评测场景](references/evaluation-scenarios.md)。
- 需要选择输出对象、格式或处理图片附件时，读取 [输出与附件指南](references/output-and-attachments.md)。
- 需要修改已有需求、管理范围或对照实现结果时，读取 [变更与交付指南](references/change-and-delivery.md)。
